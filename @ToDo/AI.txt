Create python3 code while handling all potential exceptions and errors to:
! FolderStringUpdates
- update all strings in all directories and files that contain "FolderName" with "ThisFolderName"

Create python3 code while handling all potential exceptions and errors to:
! DownloadRepoLinks
- download the content of each valid url in "Sources/sourceLinks.txt" and append it to Sources/blocklist.txt"

Create python3 code while handling all potential exceptions and errors to:
! RunPreHostlistCompiler
- run this command line command hostlist-compiler -c "Sources/blocklist.json" -o "Sources/blocklist.txt"
- if a line in Sources/blocklist.txt begins with "!" and has no characters after it, delete that entire line
- run this command line command hostlist-compiler -c "Sources/whitelist.json" -o "Sources/whitelist.txt"
- if a line in Sources/whitelist.txt begins with "!" and has no characters after it, delete that entire line

Create python3 code while handling all potential exceptions and errors to:
! FormatBlockList
- alphabetically sort "Sources/blocklist.txt"
- delete line if it starts with "!" or "#" from "Sources/blocklist.txt"
- delete line if it contains all digits and only one tld in it from "Sources/blocklist.txt"
- if any line contains the "^" character delete the "^" character and everything after it on that particular line from "Sources/blocklist.txt"
- if any line contains the "$" character delete the "$" character and everything after it on that particular line from "Sources/blocklist.txt"
- delete line if it contains any string from "Regex/Rules/strings.txt" from "Sources/blocklist.txt"
- delete line if it matches any regex pattern in "Regex/Rules/regex.txt" from "Sources/blocklist.txt"

Create python3 code while handling all potential exceptions and errors to:
! Alphabetical
scan "Sources/blocklist.txt" and sort it in the following manner:
-- take all entires beginning with the letter a and put them into "Regex/Sorted/Alphabetical/A.txt"
-- take all entires beginning with the letter b and put them into "Regex/Sorted/Alphabetical/B.txt"
-- take all entires beginning with the letter c and put them into "Regex/Sorted/Alphabetical/C.txt"
-- continue to do this for all entries from letters A-Z, placing them all into their own file

Create python3 code while handling all potential exceptions and errors to:
! ExtractRegexPatterns
- extract matches by regex pattern \b([|]?)([a-zA-Z0-9-]+)\.([a-zA-Z]{2,})([|^]?)(?=\s|$) and place those matches in "Regex/Sorted/DomainParents/extracts.txt"
- extract matches by regex pattern ^(?![a-zA-Z0-9-]+\.[a-zA-Z]{2,}$)([a-zA-Z0-9-]+\.)+[a-zA-Z]{2,}$ and place those matches in "Regex/Sorted/DomainsChildren/extracts.txt"
- extract matches by regex pattern ^[A-Za-z]+$ and place those matches in "Regex/Sorted/OnlyLetters/extracts.txt"
- extract matches by regex pattern ^([a-zA-Z]+([ _-][a-zA-Z]+){0,99998})?$ and place those matches in "Regex/Sorted/SpacesUnderscoresHyphens/extracts.txt"
- extract matches by regex pattern ^.*\*.*$ and place those matches in "Regex/Sorted/WildCards/extracts.txt"
- extract matches by regex pattern ^[0-9]+$ and place those matches in "Regex/Sorted/OnlyNumbers/extracts.txt"
- delete lines entirely that match regex pattern ^[0-9]+$ from "Sources/blocklist.txt"

Create python3 code while handling all potential exceptions and errors to:
! GetCommonPhrases
- scan "Regex/Sorted/DomainParents/extracts.txt" for the 1000 most common phrases used and place those most common phrases in "Regex/Sorted/DomainParents/common.txt"
- scan "Regex/Sorted/DomainsChildren/extracts.txt" for the 1000 most common phrases used and place those most common phrases in "Regex/Sorted/DomainsChildren/common.txt"
- scan "Regex/Sorted/SpacesUnderscoresHyphens/extracts.txt" for the 1000 most common phrases used and place those most common phrases in "Regex/Sorted/SpacesUnderscoresHyphens/common.txt"
- scan "Regex/Sorted/OnlyLetters/extracts.txt" for the 1000 most common phrases used and place those most common phrases in "Regex/Sorted/OnlyLetters/common.txt"
- scan "Regex/Sorted/OnlyNumbers/extracts.txt" for the 50 most common phrases used and place those most common phrases in "Regex/Sorted/OnlyNumbers/common.txt"
- scan "Regex/Sorted/WildCards/extracts.txt" for the 1000 most common phrases used and place those most common phrases in "Regex/Sorted/WildCards/common.txt"

Create python3 code while handling all potential exceptions and errors to:
! RunFinalHostlistCompiler
- run this command line command hostlist-compiler -c Final/regexDNSF.json -o Final/regexDNSF.txt
- if a line in Final/regexDNSF.txt begins with "!" and has no characters after it, delete that entire line
- insert "! Title: RegexDNSF ThisFolderName" to the top of Final/regexDNSF.txt

Create python3 code while handling all potential exceptions and errors to:
! RunCodeScripts
- execute the following python files in this order:
-- Code/Scripts/FolderStringUpdates.py
-- Code/Scripts/DownloadRepoLinks.py
-- Code/Scripts/RunPreHostlistCompiler.py
-- Code/Scripts/FormatBlockList.py
-- Code/Scripts/Alphabetical.py
-- Code/Scripts/ExtractRegexPatterns.py
-- Code/Scripts/GetCommonPhrases.py
-- Code/Scripts/RunFinalHostlistCompiler.py